version: "3.8"

services:
  n8n:
    image: n8nio/n8n:latest
    restart: unless-stopped
    ports:
      - "5678:5678"
    # Persists workflows and credentials using the built-in SQLite DB.
    volumes:
      - n8n_data:/home/node/.n8n

volumes:
  n8n_data: